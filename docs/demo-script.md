# Demo Package

## Produced submission cut — 2:02.40

The local master is `submission/agentic-service-dispatch-demo.mp4` (ignored from Git source because it is published separately). It is 1440×900, H.264 High / AAC-LC 48 kHz mono, English audio, fast-start MP4, and SHA-256 `597ced40dff68bd60a48838afdaee08abf6981839443baaa8f7f12066f4ff2c2`. Full decode verification passed.

The cut uses four native Chrome 151 frames from the public deployment and a 34-second moving interaction that is visibly overlaid **RECORDED INTERACTION · VISIBLE TEST ADAPTER**. The narration explicitly distinguishes native evidence from the deterministic adapter. There is no music, third-party logo, stock asset, customer data, or external write. The transcript is committed at `submission/narration.txt`. The verified cut is public at [youtu.be/ppIc0-dbmKA](https://youtu.be/ppIc0-dbmKA), with comments disabled.

## Native continuous-recording target — 2:20

Chosen concept: **The capability that did not exist.**

## Recording conditions

- Record at 1440×900, browser zoom 100%.
- Use native WebMCP in the exact supported Chrome/ChatGPT browser used for the final live URL.
- Start from Reset with five tools and a clean console.
- Keep capability count and audit log visible throughout.
- Do not cut from approval through sixth-tool appearance, commit, and revocation.
- Capture the browser version and the final getTools state separately for the submission evidence.
- If native is unavailable, stop. Do not present the Playwright/test adapter as native.

## Timed script

| Time | Screen action | Narration | Required visible proof |
| --- | --- | --- | --- |
| 0:00–0:05 | Hold initial count 5 and “Write capability absent.” | “There are five live WebMCP tools. The dangerous one does not exist.” | Native badge, count 5, five rows, write absent. |
| 0:05–0:10 | Trace the prompt ending “until I approve.” | “A prompt can ask an agent to wait, but only human approval should change its actual authority.” | One prompt; thesis. |
| 0:10–0:18 | Select **Run live 5-tool sequence**. | “This deterministic WebMCP runner calls the five registered page tools—not a simulated AI.” | “CALLS LIVE TOOLS”; audit updates. |
| 0:18–0:23 | Point across history, provider comparison, and draft. | “They load context, review history, compare providers, check availability, and create one reviewable result.” | Reviewed history; reasons for all providers. |
| 0:23–0:27 | Hold draft. | “Kairo matches, but this is explicitly a draft—not a submission.” | “DRAFT — NOT SUBMITTED,” price, slot, rationale. |
| 0:27–0:30 | Select **Approve this exact dispatch**. Do not cut. | “Human approval creates the missing sixth capability.” | Count 6 and commit_approved_dispatch. |
| 0:30–0:40 | Point from binding hash to sixth tool. | “It is bound to the canonical hash of this exact vehicle, provider, slot, price, scope, and rationale.” | Exact draft binding; exact-draft tool copy. |
| 0:40–0:47 | Point to tool schema/countdown if DevTools is included, otherwise the tool row/countdown. | “The agent can supply only the approval ID. It cannot redirect the approved fields. Authority expires after 120 seconds.” | One-time use and countdown. |
| 0:47–0:54 | Point to last audit entries. | “A nonce, idempotency key, generation, and live hash are revalidated at execution.” | Approved and capability-registered audit entries. |
| 0:54–1:00 | Select **Invoke one-time commit tool**. Do not cut. | “Now invoke the capability once.” | Button progress and successful committed result. |
| 1:00–1:06 | Hold count 5 and revoked proof. | “The result settles, the authority is consumed, and the tool unregisters. Six becomes five.” | Count 5; commit tool absent; revoked message. |
| 1:06–1:12 | Trace the capability source line and audit. | “This panel re-reads actual getTools after toolchange. It is not inferred from application state.” | getTools source proof; commit and revoke audit entries. |
| 1:12–1:20 | Select **Reset Demo** twice, deliberately. | “Reset is idempotent and verifies the exact five-tool baseline.” | Empty draft, count 5, no error. |
| 1:20–1:34 | Hold full reset screen. | “This is a fictional, browser-local reference implementation: no customer data, database, API key, AI API, or external business write.” | Fictional-data notice. |
| 1:34–1:50 | Move left → center → right. | “The user is an operations coordinator combining history, qualifications, availability, budget, deadline, and a final dispatch decision.” | Context, decision, capability structure. |
| 1:50–2:05 | Briefly show approved screenshot, then committed screenshot, or replay the live visual without relabeling it as native evidence. | “The pattern transfers to procurement, refunds, bookings, field work, and other prepare-by-agent, authorize-by-human operations.” | Visual 6 versus 5 contrast. |
| 2:05–2:15 | Return to full live screen and thesis. | “WebMCP matters because the person and agent share the page, the reviewed object, and the capability lifetime.” | Native badge, thesis, getTools panel. |
| 2:15–2:20 | End on lifecycle cue. | “Approval changed what the agent could do—then one exact action consumed it.” | “5 PREPARE → 6 APPROVE → 5 CONSUME.” |

## First-30-second pass/fail

By 0:30 a silent viewer must have seen:

1. five actual WebMCP tools;
2. the write tool absent;
3. one prompt;
4. five live tool invocations;
5. a draft explicitly not submitted;
6. human approval; and
7. the sixth one-time tool appear.

If any item is unreadable at normal playback speed, redo the recording rather than extending narration.

## Claims to avoid

- Do not call the deterministic runner an AI agent.
- Do not call Playwright screenshots native evidence.
- Do not say the prototype is production authorization.
- Do not claim saved hours, customers, revenue, market share, or integrations.
- Do not use third-party music, logos, trademarks, or stock assets.
